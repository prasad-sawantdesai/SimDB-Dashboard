name: CI/CD Pipeline

env:
  CACHE_FROM: type=gha,scope=simdb-dashboard
  CACHE_TO: type=gha,mode=max,scope=simdb-dashboard

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    name: Build, test, and package
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Get version
      id: version
      run: echo "value=$(git describe --tags --always 2>/dev/null || echo 0.0.0-unknown)" >> "$GITHUB_OUTPUT"

    - name: Lowercase repo-owner
      run: |
        # use Bash to convert to lowercase
        echo "REPO_OWNER_LOWER=${GITHUB_REPOSITORY_OWNER,,}" >>${GITHUB_ENV}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4

    # APP_VERSION is _not_ passed to lint/test targets: those stages inherit from
    # the builder stage which does not consume it, so omitting it avoids
    # unnecessary cache invalidation on every run.
    - name: Build lint target
      uses: docker/build-push-action@v7
      with:
        context: .
        target: lint
        push: false
        cache-from: ${{ env.CACHE_FROM }}
        cache-to: ${{ env.CACHE_TO }}

    - name: Build test target
      uses: docker/build-push-action@v7
      with:
        context: .
        target: test
        push: false
        cache-from: ${{ env.CACHE_FROM }}
        cache-to: ${{ env.CACHE_TO }}

    # APP_VERSION is embedded into the static assets at build time
    - name: Build build target image
      uses: docker/build-push-action@v7
      with:
        context: .
        target: build
        push: false
        load: true
        tags: simdb-dashboard:build
        build-args: APP_VERSION=${{ steps.version.outputs.value }}
        cache-from: ${{ env.CACHE_FROM }}
        cache-to: ${{ env.CACHE_TO }}

    - name: Build service image
      uses: docker/build-push-action@v7
      with:
        context: .
        target: service
        push: false
        load: true
        tags: simdb-dashboard:service
        build-args: APP_VERSION=${{ steps.version.outputs.value }}
        cache-from: ${{ env.CACHE_FROM }}
        cache-to: ${{ env.CACHE_TO }}

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ env.REPO_OWNER_LOWER }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Push service image
      uses: docker/build-push-action@v7
      with:
        context: .
        target: service
        push: true
        load: true
        tags: ghcr.io/${{ env.REPO_OWNER_LOWER }}/simdb-dashboard:service-${{ steps.version.outputs.value }}
        build-args: APP_VERSION=${{ steps.version.outputs.value }}
        cache-from: ${{ env.CACHE_FROM }}
        cache-to: ${{ env.CACHE_TO }}

    # - name: Extract frontend assets
    #   run: |
    #     mkdir -p dist
    #     docker rm -f tmp_dist_container >/dev/null 2>&1 || true
    #     docker create --name tmp_dist_container simdb-dashboard:build >/dev/null
    #     docker cp tmp_dist_container:/app/dist ./dist
    #     docker rm tmp_dist_container >/dev/null

    # - name: Export service image artifact
    #   run: |
    #     mkdir -p artifacts
    #     docker image inspect simdb-dashboard:service >/dev/null 2>&1
    #     docker save -o artifacts/simdb-dashboard.tar simdb-dashboard:service

    # - name: Upload static assets artifact
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: dist
    #     path: dist/
    #     retention-days: 7

    # - name: Upload service image artifact
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: simdb-dashboard
    #     path: artifacts/simdb-dashboard.tar
    #     retention-days: 7
